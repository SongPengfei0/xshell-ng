# Keep npm cache inside the workspace. The folder is ignored by Git.
cache=.npm-cache

# Change or remove this mirror locally if the official Electron CDN is preferred.
electron_mirror=https://npmmirror.com/mirrors/electron/
